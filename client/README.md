# Bolahh — Client

React + Vite frontend for [bolahh.com](https://bolahh.com).

See the [root README](../README.md) for full setup, deployment, and documentation.

## Quick start

```bash
npm install
npm run dev
```

App runs at `http://localhost:5173`. Requires a `.env` file — see the root README for the required variables.
